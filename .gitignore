# 회사 프로필·자격증명 — 절대 커밋 금지
company-profile.yaml
company-profile.local.yaml
marketing-sources.yaml
*.local.yaml
.env
.env.*
!.env.example

# SNS 세션·쿠키
sessions/
cookies/
auth/
# harness/examples/auth/ 는 placeholder (실 토큰 아님) — 커밋 허용.
!harness/examples/auth/
!harness/examples/auth/*.json

# 생성된 이미지·빌드 산출물
out/

# 캠페인 폴더 구조는 유지하되 생성된 드래프트·타임스탬프 파일만 제외
posts/campaigns/**/draft.yaml
posts/campaigns/**/draft.md
posts/campaigns/**/[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]-*.yaml
posts/campaigns/**/[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]-*.md

# symlink 자동 생성물 (sync-posts.mjs)
posts/by-channel/

# 반복 실행 슬롯 (개인 데이터)
posts/slots.yaml

# 학습된 사용자 선호도 (개인 데이터, approve/reject 시 자동 누적)
posts/preferences.yaml

# 개발 중 디버깅 probe 스크립트 (커밋 X)
harness/bin/_temp-*

# 리버스 엔지니어링 분석 자료 (큰 파일 + 외부 코드)
_research/

# 회사 자체 자산 (제품 사진 등)
assets/

# 백업 파일 (회사 프로필, draft 인라인 편집 백업, original 보존본)
*.bak.yaml
*.bak.json
*.yaml.bak
*.json.bak
*.original.yaml

# 발행 진행 로그 (휘발성)
posts/campaigns/**/publish.log

# 노드/툴링
node_modules/
.pnpm-store/
dist/
build/
*.log
.DS_Store

# IDE
.vscode/
.idea/
*.swp

# Claude Code 로컬 상태
.claude/state/
.claude/cache/

out/.update-check
.claude/
